import Calculator from './Calculator';

const calculator = new Calculator();

// calculator.calculate: number(operator: [+, -, *, /], operand1: number || string, operand2: number || string) 형식으로 연산 결과를 반환합니다.
calculator.calculate('+', 1, 2);
