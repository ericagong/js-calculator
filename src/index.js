import Calculator from './Calculator.js';

const calculator = new Calculator();

// type operator = '+' | '-' | '*' | '/'
// type operand = number | numberTypeString
// calculator.calculate: number(operator, operand1, operand2)
calculator.calculate('+', 1, 2);
